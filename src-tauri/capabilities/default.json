{
  "identifier": "default",
  "description": "Default capabilities for ntfy desktop",
  "windows": ["main", "settings", "welcome", "settings-*"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
